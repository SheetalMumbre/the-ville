import { PREPARE_STRING_RESOURCES_EDIT_MODE } from "./string-resources.consts";

export const prepareStringResourcesEditMode = (isEnabled) => ({ type: PREPARE_STRING_RESOURCES_EDIT_MODE, isEnabled });
